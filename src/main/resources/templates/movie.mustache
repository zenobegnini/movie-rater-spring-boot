{{>header}}

<div class="row">
    <div class="col s6">
        <div class="card">
            <div class="card-content">
                <span class="card-title">{{movie.title}}</span>
                <p>{{movie.director}}</p>
            </div>
            {{#actors}}
                <div>
                    <p>{{name}} {{surname}}</p>
                </div>
            {{/actors}}
            <div>
                <p>Votes: {{votes}}</p>
            </div>

        </div>
    </div>

{{>footer}}

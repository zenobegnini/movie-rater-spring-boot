{{>header}}

<div class="row">
    <div class="col s6">
        <div class="card">
            <div class="card-content">
                <span class="card-title">{{actor.name}} {{actor.surname}}</span>
            {{#movie}}
                <div>
                    <a href="http://localhost:8080/movie/{{id}}"><p>{{title}}</p></a> <p>{{votes}}</p>
                </div>
            {{/movie}}
        </div>
    </div>

{{>footer}}
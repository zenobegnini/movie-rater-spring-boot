{{>header}}

<div class="row">
    <div class="col s6">
        <div class="card">
            <div class="card-content">
                <span class="card-title">{{actor.name}} {{actor.surname}}</span>
            {{#movie}}
                <div>
                    <p>{{title}} {{votes}}</p>
                </div>
            {{/movie}}
        </div>
    </div>

{{>footer}}